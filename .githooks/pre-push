#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "${REPO_ROOT}"

echo "Running Semgrep push gate..."
bash scripts/semgrep-scan.sh

echo "Running TruffleHog push gate..."
bash scripts/trufflehog-prepush.sh

echo "Security push gates passed."
